# 中药-成分-靶点-微生物-疾病 数据集总览

> 文中所有数量都是**从实际下载到的文件里数出来的**，与论文口径不符之处均已标注。
> 数据在本仓库 `datasets` 分支，超过 90MB 的在 Releases。

## 一、整体情况

最初圈定 9 个库，**实际拿到 5 个**。体量按**实体数量**计：

| 库 | 位置 | 实体数量（实测） | 怎么拿到的 |
|---|---|---|---|
| **SymMap** | `symmap/` | 703 草药 · 27,690 成分 · 20,965 靶点 · 14,434 疾病 · 2,364 中医症状 · 1,148 现代症状 · 233 证候 | 官网直链（只有 http，https 拒连） |
| **dbPTH** | `dbpth/` + Releases | 见 2.2（论文数字与实测差异极大） | 官网 `Download.php` 的分片 |
| **Disbiome** | `disbiome/` | 10,866 条菌-病关联 · 1,537 微生物 · 323 疾病 | BioThings 镜像（官网 API 是坏的） |
| **TCMSP** | `tcmsp/` | 502 草药 · 13,729 成分 · 3,339 靶点 · 867 疾病 | `browse.php` 页面内联的整表 |
| **TCMID** | `tcmid/` | 8,203 草药 · 32,281 成分 · 46,914 复方 · 13,403 蛋白 · 211,152 条成分-蛋白 | Wayback 存档（**是 v1.0，不是 2.0**） |

没拿到的 4 个：

- **HERB** —— 下载接口是好的，但它指向的文件在服务端已经被删了。
  证据是同一个接口对不同路径写法给出两种错误：格式不对的回 `unavailable path`，
  格式对的回 `file_path dose not exists`。Wayback 里这个域唯一存档到的"数据文件"是 `robots.txt`。
- **HIT 2.0** —— 课题组自己的 Resources 页上给的唯一地址是 `hit2.badd-cao.net`，
  而那只是个指向 `badd-cao.net:2345` 的 frameset，那个端口 Connection refused。服务停了。
- **MDIPID** —— `mdipid.idrblab.net` 持续返回 HTTP 500，Wayback 零存档。
- **MicrobeTCM** —— Vue 单页应用，页面里没有任何数据链接；它的 `/api/index/*` 看着像接口，
  实际是**文件上传**用的，不是取数的。

丢掉 MicrobeTCM 和 MDIPID 的代价很具体：**"中药→微生物"这条边整个没了**，
这决定了知识图谱能做成什么样，第三节会讲。

---

## 二、各个数据集

### 2.1 SymMap

**一句话**：中药领域少有的把"中医症状"和"现代医学症状"做了人工映射的库，
所以它能同时从分子和症状两头连接中西医。

- **网址**：http://www.symmap.org/
- **所有者**：中国中医科学院、清华大学等（NAR 2019, 47(D1):D1110）
- **格式**：Excel `.xlsx`，每个实体一个文件；另配一份 `key file` 是站内检索词表，做分析用不上
- **实体数量**（实测）：

  | 表 | 内容 | v1.0 | v2.0 |
  |---|---|---:|---:|
  | SMHB | 草药 | 499 | **703** |
  | SMIT | 成分 | 19,595 | **27,690** |
  | SMTT | 靶点 | 4,302 | **20,965** |
  | SMDE | 疾病 | 5,235 | **14,434** |
  | SMTS | 中医症状 | 1,717 | **2,364** |
  | SMMS | 现代医学症状 | 961 | **1,148** |
  | SMSY | 证候 | — | **233** |

**真实样例 —— 草药表 SMHB v1.0 的第一条记录**（`samples/symmap_v1_0_SMHB.real.tsv`，
共 14 列，此处纵向展开以便看清）：

| 字段 | 值 |
|---|---|
| `Herb_id` | 1 |
| `Chinese_name` | 矮地茶 |
| `Pinyin_name` | Ai Di Cha |
| `Latin_name` | Ardisiae japonicae |
| `English_name` | Japanese Ardisia Herb |
| `Properties` | Mild,Pungent,Bitter |
| `Meridians` | Lung,Liver |
| `Function` | 1. To eliminate phlegm and relieving cough.2. To promote diuresis to deprive dampness. 3. … |
| `Class_Chinese` | 止咳平喘药 |
| `Class_English` | Antitussive Antiasthmetics |
| `UsePart` | — |
| `TCMID_id` | 5072 |
| `TCM-ID_id` | 2 |
| `TCMSP_id` | 1 |

**真实样例 —— 疾病表 SMDE v1.0 的第一条记录**（共 9 列，注意它带一整排交叉引用）：

| 字段 | 值 |
|---|---|
| `Disease_id` | 5035 |
| `Disease_Name` | Congenital Adrenal Hyperplasia Due To 11-Beta-Hydroxylase Deficiency |
| `Disease_definition` | Congenital adrenal hyperplasia due to 11 beta-hydroxylase (CYP11B1) deficiency is a rare f… |
| `MeSH_id` | C535978 |
| `OMIM_id` | 202010 |
| `Orphanet_id` | 90795 |
| `ICD10CM_id` | E25.0 |
| `UMLS_id` | C0268292 |
| `MedDRA_id` | 10000002 |

优点是**交叉引用做得最全**，这点被低估了。靶点表自带 `HIT_id` 和 `TCMSP_id`，
疾病表自带 `UMLS_id` + `MeSH_id` + `ICD10CM_id` + `MedDRA_id`——
等于白送了一张跨库映射表。

⚠️ **但要看填充率，不能只看列存在**。实测各桥接列的实际填充情况：

| 列 | v1.0 | v2.0 | 能不能用 |
|---|---:|---:|---|
| `SMDE.UMLS_id` | 94.9% | **98.2%** | ✅ 疾病对齐的主键 |
| `SMDE.MedDRA_id` | 4.6% | **2.0%** | ❌ 太稀疏，接不了 Disbiome |
| `SMTT.Gene_symbol` | 100% | **100%** | ✅ 靶点对齐的主键 |
| `SMTT.UniProt_id` | 40.6% | **8.3%** | ❌ v2.0 里几乎没有 |
| `SMIT.PubChem_CID` | 21.9% | 42.0% | ⚠️ 勉强可用 |
| `SMIT.Molecule_structure` | **0%** | **0%** | ❌ 没有 SMILES，做不了结构对齐 |

注：

1. **下载页只有实体表，没有任何关系表**。我核过下载页全部 26 个链接，
   就是 6–7 张实体表加各自的 key file，再没别的。也就是说
   **光靠批量下载建不出 SymMap 的网络**，拿到的只是节点属性。
2. **论文里引用的数字全是 v1.0 的**。很多文章写"SymMap 收录 499 味草药、4,302 个靶点"，
   那是 v1.0；v2.0 靶点已经到 20,965，差了近 5 倍。**用 v2.0**，引用时注意标版本。
3. v2.0 多了 `Version` / `Suppress` / `Link_*_id` 三个管理字段，
   **载入时要先按 `Suppress` 过滤掉废弃记录**，否则统计会偏高。
4. v1.0 的 `Symptom_pinyin_name` 在 v2.0 改名成了 `Symptom_pinYin`，跨版本合并会踩。

---

### 2.2 dbPTH 1.0

**一句话**：中药成分-靶点互作收录量最大的库，规模比同类库高几个量级。

- **网址**：http://dbpth.biocuckoo.cn/ （下载页是 **`/Download.php`**，写成 `/Download/` 会 403）
- **所有者**：华中科技大学 BioCuckoo 课题组 与 北京中医药大学（GPB 2026）
- **格式**：zip，解压后是制表符分隔的 `.txt`
- **实体数量**：**论文口径与实测差异极大**，两者都列出：

  | 指标 | 论文口径 | **实测（全 9 个分片）** |
  |---|---:|---:|
  | 互作记录 | 165,967 | **43,288,019 行** |
  | 唯一 成分-蛋白 对 | — | **36,846,167** |
  | 蛋白（UniProt） | 27,981 | **2,683,937** |
  | 成分（PubChem） | 4,856 | **4,856** ✅ 完全一致 |
  | 物种 | 8 | **1,139** |

  **成分数与论文分毫不差（4,856），但蛋白数和物种数差了两三个量级。**
  合理的解释是：论文的 27,981 指人类靶点，而实际文件把这些靶点在 1,139 个物种里的
  **同源蛋白全部展开**了——这正是论文提到的"跨物种同源靶点映射"。
  但这只是推断，官方未就此说明。

- **人类子集**（`Specie = Homo sapiens`，建图实际要用的）：

  | 指标 | 数量 |
  |---|---:|
  | 记录行数 | **83,751**（占全库 0.19%） |
  | 唯一蛋白 | **6,526** |
  | 唯一基因 | **6,467** |
  | 唯一成分 | **4,850** |
  | 唯一 成分-蛋白 对 | **74,319** |

**这里有个很实用的发现**：下载页其实列了 **1,325 个 zip 分片**，
按蛋白分组（`ProtGrp/`）和按成分分类（`IngCls/`）两种切法。
**按 ProtGrp 取 9 个分片就覆盖全库，根本不用下 21.4 GB 的整包。**
要建数据库的话直接用 `PTH.sql.zip` 更省事。

**真实样例 —— 一条互作记录**（`samples/dbpth_Secreted_Protein.real.tsv`，共 9 列）：

| 字段 | 值 |
|---|---|
| `dbPTH ID` | PTHT0000272 |
| `Protein Accession` | A0A0G2JGF1 |
| `Protein Name` | Interleukin 6 receptor, alpha |
| `Gene` | Il6ra |
| `Ingredient` | Cysteine |
| `PubChem` | 5862 |
| `Chemical Subtype` | Small peptides |
| `Protein Subgroups` | Secreted_Protein@Membrane_Receptor |
| `Specie` | Mus musculus (Mouse) |

字段比论文描述的**精简得多**。我原以为会有活性值、实验方法、PMID、同源组 ID，
**这些一个都没有**，只有 9 列。这直接改变了它的定位：
**dbPTH 告诉你"某成分和某蛋白有互作"这个事实，但不告诉你强度和证据细节。**
要 IC50/Kd 这类活性值，得靠 HIT 2.0（而它已经停服了）。

注：

1. **物种混杂得极其厉害**。实测 **1,139 个物种，人类只占 0.19%**，
   数量最多的反而是青鳉、鲤鱼、山羊、墨西哥丽脂鲤这些。
   **做人类研究必须先按 `Specie` 过滤**，否则人类数据会被彻底淹没。
   同源关系不是靠某个字段给的，而是同一个 `dbPTH ID` 下挂多个物种的 `Protein Accession`。
2. `Protein Subgroups` 是 `@` 分隔的多值字段，不 split 的话按分组统计会漏。

---

### 2.3 Disbiome

**一句话**：专做"某种菌在某种病里变多还是变少"的库，标准化程度是这批里最好的。

- **网址**：https://disbiome.ugent.be/
- **所有者**：比利时根特大学（BMC Microbiology 2018, 18:50）
- **形式**：REST API
- **格式**：JSON（BioLink 风格的嵌套结构）
- **实体数量**（实测）：**10,866 条菌-病关联 · 1,537 个微生物（按 taxid）· 323 种疾病（按 MedDRA）**；
  方向分布 `increased` 5,713 / `decreased` 5,153

**取数方式要特别说一下**：官网那套 `/api/disbiome/experiments` 接口**是坏的**——
它对任何路径都回 HTTP 200 + 网站首页的 HTML。只看状态码会以为下载成功了，
实际存下来一堆 HTML。真正能用的是 BioThings 镜像：

```
https://pending.biothings.io/disbiome/query?q=__all__&fetch_all=true
```

单次查询上限 1000 条，**必须用 `fetch_all` 拿 scroll_id 翻页**，不然只拿到第一页还以为是全量。

**真实样例 —— 一条实验观察记录**（`samples/disbiome_experiments.real.tsv`，已扁平化，共 13 列）：

| 字段 | 值 |
|---|---|
| `organism` | oribacterium |
| `taxid` | 265975 |
| `rank` | genus |
| `disease` | invasive colorectal cancer |
| `meddra_id` | 10052358 |
| `meddra_level` | preferred_term |
| `qualifier` | decreased |
| `sample_site` | feces |
| `method` | 16S rRNA sequencing |
| `host` | human |
| `control` | early colorectal cancer patient |
| `pmid` | 31609493 |
| `doi` | 10.1111/jgh.14868 |

标准化是真的好：疾病挂 MedDRA、微生物挂 NCBI Taxonomy，还带 PMID 和 DOI。
尤其原始 JSON 里的 `subject.lineage` 直接给了完整的 NCBI 分类谱系数组，
**要把种级记录上卷到属级或门级不用另外查分类库**，这个字段很省事。

注：

1. **粪便样本只占一半**（5,500/10,866），另外近一半来自口腔、皮肤、泌尿生殖道、
   支气管肺泡灌洗液等。**做肠道菌群研究必须先按 `sample_site` 过滤**，
   否则会把口腔菌群的结论混进来。
2. **检测方法名没有归一化**。`16S rRNA sequencing`、`16S rDNA pyrosequencing`、
   `16S rRNA pyrosequencing`、`MiSeq sequencing` 实际都是 16S 扩增子测序，
   按方法分层分析前得自己合并同类项。
3. 方向只有 `increased` / `decreased` 两极，**没有效应量**。
   同一个菌-病对经常有多条来自不同研究的记录，方向还可能打架——这点在建图时要处理。

---

### 2.4 TCMSP 2.3

**一句话**：中药网络药理学的事实标准入门库，价值不在数据量而在它是唯一系统提供 ADME 参数的。

- **网址**：https://www.tcmsp-e.com/
- **所有者**：西北农林科技大学（J Cheminform 2014, 6:13）
- **形式**：网页（`browse.php` 把整表内联在页面里，**不需要爬虫**）
- **格式**：页面内联 JSON（我们另存了一份 TSV）
- **实体数量**（实测）：草药 **502** · 成分 **13,729** · 靶点 **3,339** · 疾病 **867**；
  套用领域惯例阈值 OB≥30% 且 DL≥0.18 后剩 **2,583** 个活性成分（18.8%）

**真实样例 —— 成分表里的 quercetin**（`samples/tcmsp_ingredients.real.tsv`，共 15 列）：

| 字段 | 值 |
|---|---|
| `MOL_ID` | MOL000098 |
| `molecule_ID` | 98 |
| `molecule_name` | quercetin |
| `mw` | 302.250 |
| `hdon` | 5 |
| `hacc` | 7 |
| `alogp` | 1.504 |
| `halflife` | 14.400548 |
| `ob` | 46.4333481195 |
| `caco2` | 0.04842 |
| `bbb` | -0.76890 |
| `dl` | 0.27525 |
| `FASA` | 0.38453352 |
| `tpsa` | 131.36 |
| `rbn` | 1 |

**真实样例 —— 草药表**（共 5 列）：

| `herb_cn_name` | `herb_pinyin` | `herb_en_name` | `child_cn_name` | `child_en_name` |
|---|---|---|---|---|
| 矮地茶 | Aidicha | Ardisiae Japonicae Herba | 止咳平喘药 | Antitussive Antiasthmetics |
| 艾叶 | Aiye | Folium Artemisiae Argyi | 温经止血药 | Channel-Warming Hemostatic |
| 安息香 | Anxixiang | Benzoinum | 开窍药 | Resuscitative Stimulant; Resuscitative M… |

**真实样例 —— 疾病表**（共 5 列，自带 ICD 编码）：

| `disease_ID` | `DIS_ID` | `disease_name` | `ICD9` | `ICD10` |
|---|---|---|---|---|
| 1 | DIS00001 | Abdominal aortic aneurysm | 442 | 172 |
| 2 | DIS00002 | Abortion, Spontaneous | — | — |
| 3 | DIS00003 | Achondroplasia | 756.4 | Q77.4 |

ADME 参数是它不可替代的地方——OB（口服生物利用度）、DL（类药性）、Caco-2、BBB、
半衰期等十来项，领域里人人写的那句"以 OB ≥ 30%、DL ≥ 0.18 筛选活性成分"就出自这里。
我抽查了几个知名成分，quercetin OB=46.43/DL=0.275、berberine OB=36.86/DL=0.778，
**和 TCMSP 公布值一致**，数据是对的。

注：

1. **所有数值字段都是字符串**（`"ob":"46.4333481195"`），
   直接拿去比大小会静默出错——字符串比较下 `"9" > "30"`。必须先转数值：

   ```python
   df[["ob","dl","mw","caco2","bbb"]] = df[["ob","dl","mw","caco2","bbb"]].apply(
       pd.to_numeric, errors="coerce")
   ```

   13,729 个成分的 `ob`/`dl` 全都能成功转换，没有缺失；但 `halflife` 有空值，要容错。
2. 这四张是**实体表，不含关系**。成分↔靶点、靶点↔疾病的配对数据在
   `tcmspsearch.php?qr=<名称>&qsr=herb_en_name&token=<token>` 的逐草药结果页里，本次没抓。
3. 靶点有一部分是模型预测的，做机制论证时该和 dbPTH 的实验证据交叉验证。

---

### 2.5 TCMID（v1.0）

**一句话**：唯一系统收录**复方**的库，而且它是这批里少数直接提供关系数据的。

- **网址**：原址 `tcmid.org` **已经不是这个库了**——域名过期后被人注册成了一个叫
  "My Sheep Gate" 的慈善机构网站。历史镜像 `megabionet.org/tcmid/` 超时。
  我们的数据来自 **Internet Archive 存档**，来源和快照时间记在 `tcmid/provenance.json`。
- **所有者**：上海交通大学等（TCMID 2013 / 2.0 见 NAR 2018, 46(D1):D1117）
- **形式**：存档恢复的静态文件
- **格式**：zip，解压后是制表符分隔的 `.csv`
- **实体数量**（实测）：

  | 文件 | 数量 | 内容 |
  |---|---:|---|
  | `prescription.csv` | **46,914** 条复方 | 中文组成、主治、用法、出处 |
  | `compounds.csv` | **32,281** 个成分 | STITCH ID、PubChem ID、分子式、SMILES |
  | `compound_protein.csv` | **211,152** 条成分-蛋白（涉及 4,939 成分 / **13,403** 蛋白） | UniProt |
  | `herb.csv` | **8,203** 味草药 | 药性、归经、用部、功效、主治、所含成分 |

> ⚠️ **这是 v1.0，不是 2.0。** 存档快照是 2015 年，文件日期 2012。
> 论文里 2.0 的统计数字（18,203 成分等）**不适用于这批文件**。

**真实样例 —— 草药表的一条记录**（`samples/tcmid_herb.real.tsv`，共 10 列）：

| 字段 | 值 |
|---|---|
| `Pinyin Name` | TIAN MEN DONG |
| `Chinese Name` | 天门冬 |
| `English Name` | Cochinchinese Asparagus |
| `Latin Name` | Asparagus cochinchinensis  [Syn.  Asparagus lucidus ] |
| `Properties` | Sweet, bitter,Extremely cold |
| `Meridians` | Lung,kidney |
| `Use Part` | tuberoid |
| `Effect` | To enrich  yin and moisten dryness, clear lung and downbear fire. |
| `Indication` | Yin vacuity fever, cough with blood ejection, lung wilting, pulmonary  welling abscess, sw… |
| `Ingredients` | 1908,E\|14018,T5787\|14016,E\|13838,E\|23344,M1053\|24126,M1053\|6089,E\|24128,M1053\|1907… |

**真实样例 —— 复方表**（共 7 列）：

| `Pinyin Name` | `Chinese Name` | `Composition` | `Indication` | `Method` | `Pinyin composition` | `Reference` |
|---|---|---|---|---|---|---|
| JIA WEI CAN XIA TANG | 加味参夏汤 | 人参1两半，半夏1两半，肉桂1两，甘草（炙）5钱，乳香3钱。 | 七情相干，眩晕欲倒；又治七情之气郁于心腹，不可忍，脉沉迟者。 | 每服5钱，加生姜5片，水煎服。 | REN CAN,BAN XIA,ROU GUI,GAN CAO,RU XIANG… | 《赤水玄珠》卷十六。 |
| HUANG QI WAN | 黄耆丸 | 黄耆1两（锉），木香1两，漏芦1两，枳壳1两（麸炒微黄，去瓤），玄参1两，犀角屑… | 瘰疬结肿生脓。 | 每服20丸，空心及晚食前以粥饮送下。 | HUANG QI,MU XIANG,LOU LU,ZHI KE,XUAN CAN… | 《圣惠》卷六十六。 |

**真实样例 —— 成分表**（共 6 列）：

| `ID` | `STITCH ID` | `PubChem ID` | `Formula` | `name` | `SMILE String` |
|---|---|---|---|---|---|
| 1 | CID005315111 | 5315111 | Cl2O4 | abamagenin | CC1COC2(CC1C(Cl)Cl)C(C3C(O2)CC4C3(CCC5C4… |
| 2 | NA | Not Available | C20H34O3 | (+)-(4→2)-abeo-kolavelool-3-oicacid | Not Available |

**真实样例 —— 成分-蛋白关系表**（共 3 列，原文件无表头）：

| `compound_id` | `UniProt` | `source` |
|---|---|---|
| 20001 | Q4L235 | STITCH |
| 20001 | P60981 | STITCH |
| 20001 | Q12800 | STITCH |

对建图来说，**TCMID 的价值被严重低估了**——它是这 5 个库里少数直接给"边"的。
`herb.csv` 的 `Ingredients` 列就是草药→成分的边，`compound_protein.csv` 就是成分→蛋白的边。
我验过它的内部 ID 连接，**干净得出乎意料**：

- herb→ingredient 共 **59,805 条引用，100% 能解析**到 `compounds.csv`
- `compound_protein.csv` 涉及的 **4,939 个成分 ID，100% 能对上**

注：

1. **成分-蛋白的证据等级低**。来源列显示 **205,939 条来自 STITCH、5,213 条来自 Textmining**，
   而 STITCH 本身就混合了预测、共表达和文本挖掘。
   **这 21 万条边不能和 dbPTH 的实验验证边同等对待**，建图时必须分权重。
2. **草药表的注释非常稀疏**。8,203 行里只有 **1,851 行**有中文名，
   有药性的更少，只有 **848 行**，其余大量是 `NA`。
   所以**别指望用它做药性/归经分析**——那部分该用 SymMap。
   但 **`Ingredients` 列是 8,203 行全有的**，作为"边"的来源是完整的，
   稀疏的只是节点属性。这正好符合它在图里的定位：提供边，属性交给 SymMap。
3. **只有 36.9% 的成分有 PubChem ID**（11,896/32,281），直接影响跨库对齐。
   注意 SymMap 没有可用的 SMILES（`Molecule_structure` 两版都是 0%），
   所以**没法靠结构算 InChIKey 兜底**。

---

## 三、融合成知识图谱

### 3.1 数据筛选清洗

清掉用不上的数据，把 5 个库统一成同一套中间格式，后面所有步骤都基于这套格式。

**要清掉的**：

- dbPTH 的非人类记录（实测 1,132 个物种，人类只占 0.2%，不过滤会淹没整张图）
- SymMap v2.0 里 `Suppress` 标记为废弃的行
- TCMID `herb.csv` 中大量字段为 `NA` 的行（只保留其 `Ingredients` 列作边用）
- Disbiome 非肠道样本（若研究范围限定肠道；`sample_site` 里粪便只占一半）

**统一成两张中间表**——所有库清洗后都落成这个结构，加新库只需再写一个适配器：

**节点表 `nodes.csv`**

| 列 | 含义 | 示例值（真实数据） |
|---|---|---|
| `node_id` | 全局唯一主键，`<类型>:<命名空间>:<ID>` | `INGR:PUBCHEM:5280343` |
| `label` | 节点类型 | `Ingredient` |
| `name` | 显示名 | `quercetin` |
| `xrefs` | 跨库 ID，`k=v` 分号分隔 | `pubchem=5280343;tcmsp=MOL000098` |
| `props` | 属性，`k=v` 分号分隔 | `ob=46.4333481195;dl=0.27525;mw=302.250` |
| `source` | 来自哪个库 | `TCMSP` |

**边表 `edges.csv`**

| 列 | 含义 | 示例值（真实数据） |
|---|---|---|
| `src` | 起点 node_id | `INGR:PUBCHEM:5280343` |
| `rel` | 关系类型 | `TARGETS` |
| `dst` | 终点 node_id | `TGT:SYMBOL:PTGS2` |
| `evidence` | 证据等级 | `experimental` |
| `source` | 来源库 | `dbPTH` |
| `weight` | 置信度 0–1 | `1.0` |

一条完整的示例链（真实实体）：

```
HERB:SYMMAP:1          [矮地茶]      --CONTAINS-->
INGR:PUBCHEM:5280343   [quercetin]   --TARGETS-->
TGT:SYMBOL:PTGS2       [PTGS2]       --ASSOCIATED_WITH-->
DIS:UMLS:C0009402      [结直肠癌]     <--ASSOCIATED_WITH--
MIC:NCBI:851           [Fusobacterium nucleatum]
```

### 3.2 实体对齐

分三层，**能用上一层解决的绝不用下一层**——越往下越不可靠、越贵。

#### 3.2.1 第一层：标准 ID 精确匹配（零成本，优先用）

| 实体类型 | 标准 ID | 说明 |
|---|---|---|
| 化合物 | PubChem CID | 各库提取后直接 JOIN |
| 靶点 | UniProt AC / 基因符号 | dbPTH 同时有两者，可当映射表 |
| 疾病 | UMLS CUI / MeSH / MedDRA | SymMap `SMDE` 的 UMLS 填充率 98.2% |
| 微生物 | NCBI Taxonomy ID | Disbiome 专用，还带完整 lineage |

这层用 SQL JOIN 就能完成，结果可复现、可审计。**绝不能让大模型插手这一层。**

#### 3.2.2 第二层：别名表模糊匹配

对没有标准 ID 的实体建别名/同义词表：

- 用 `rapidfuzz` 做字符串相似度
- 规则预处理：去盐型后缀（HCl、Na）、去立体化学标记（(R)/(S)）、
  去拉丁名里的 `[Syn. ...]` 括注、中文繁简转换
- 阈值：≥0.85 自动合并，0.7–0.85 进人工复核队列，<0.7 丢弃

#### 3.2.3 第三层：大模型语义对齐

名称不同但语义相同（如 `invasive colorectal cancer` vs `Colorectal Carcinoma`）时，
前两层必然失败，这时才用大模型。

**⚠️ 关键：先量清楚每类还剩多少，再决定用不用大模型。** 本项目实测：

| 对齐任务 | ID 直接对上 | 加规范化名后 | 剩余待解 | 结论 |
|---|---:|---:|---:|---|
| **疾病** Disbiome→SymMap | 11 / 323 (3.4%) | 122 (37.8%) | **201 个** | ✅ 适合用大模型 |
| **成分** TCMID→SymMap | 2,212 | 8,360 | **14,237 个** | ❌ 不该用 |
| **草药** TCMID→SymMap | — | 61 / 8,203 (0.7%) | — | ❌ 不是匹配问题 |

**只有疾病这一类适合**：残差仅 201 个，规模小、成本可忽略，而且这条边是连通
中药侧与微生物侧的**唯一通路**，不解决整张图就是断的。

**成分不该用**：残差 14,237 个，规模大 70 倍；更要命的是同分异构体、立体异构体名字极像，
大模型容易给出**似是而非**的匹配，化学实体错了整条通路就废了。
正确做法是拿名称去查 **PubChem PUG REST** 批量解析成 CID——确定性、可复现、免费。

**草药 0.7% 的命中率是个陷阱**：看着像匹配失败，实际是**覆盖问题**——
SymMap 只收 703 味药，TCMID 有 8,203 味，大部分在 SymMap 里压根不存在。
这时让大模型"努力匹配"，它一定会强行造出错误对应。
正确做法是**以 TCMID 当草药主表，SymMap 只作注释补充**。

**用大模型时的两条纪律**：

1. **只做"从候选里选"，不做"生成 ID"**。先用向量检索取 top-10 候选，
   让模型选 + 给置信度 + 给理由，输出落成可审计的表。
2. **低置信度的不进图**，单独列出人工复核。宁可少一条边，不要一条错边。

**可选框架**（以下为备选方案，**未经本项目核实**，采用前请自行验证）：

- EARAG —— 先用检索器召回候选实体，再构造 Prompt 让 LLM 判断是否等价并给解释
- LEA —— 无需标注数据，把结构信息文本化后在共享嵌入空间做检索对齐
- 不想用 LLM 的传统方案：OpenEA（基于知识图谱嵌入）、SILK（基于规则的链接引擎）

### 3.3 关系补全

对齐完成后跨库关系自然拼接，剩余缺失的关系分两类。

#### 3.3.1 共享实体桥接（规则推理，不引入新错误）

通过共享的 Compound（PubChem CID）和 Target（UniProt / 基因符号）把各库的关系链拼起来：

```
TCMID:   草药A — 成分X
dbPTH:   成分X — 靶点P
→ 推出: 草药A — 成分X — 靶点P
```

这类推理不引入新事实，只是把已有边接起来，安全。

#### 3.3.2 跨域推理补全（需大模型或外部库）

当前 5 个库**缺「靶点→疾病」和「中药→微生物」两类边**，导致图是断的。两条补法：

1. **抓 TCMSP 关系页**（推荐，成本最低）：
   `tcmspsearch.php?qr=<草药英文名>&qsr=herb_en_name&token=<token>`，
   token 可从 `browse.php` 页面现抓。502 味草药限速跑一轮约 10 分钟。
2. **外部库补充**：STRING（蛋白互作）、STITCH（化合物-蛋白）、CTD（化合物-疾病）。

多跳推理（A 成分→靶点 P，P 与疾病 D 相关 → 推断 A 可能作用于 D）产生的边
**必须标注 `evidence=inferred`**，与实验验证的边分开存储，查询时可过滤。

### 3.4 图谱结构与证据分级

| 边 | 来源 | 实测规模 | 证据等级 |
|---|---|---:|---|
| `Herb -CONTAINS-> Ingredient` | TCMID `herb.csv` | 59,805 | curated |
| `Ingredient -TARGETS-> Target` | **dbPTH**（人类子集） | 见 2.2 | **experimental** |
| `Ingredient -TARGETS-> Target` | TCMID `compound_protein.csv` | 211,152 | **predicted** |
| `Target -ASSOCIATED_WITH-> Disease` | TCMSP 关系页（待抓） | — | curated |
| `Microbe -ASSOCIATED_WITH-> Disease` | Disbiome | 10,866 | observational |

**dbPTH 的实验证据边与 TCMID 的预测边绝不能合并成一种边**——
TCMID 那 21 万条里 97.5% 来自 STITCH、2.5% 来自文本挖掘，混在一起会把实验证据稀释掉，
下游任何富集分析都不可信。

Disbiome 同一「菌-病」对常有多篇研究且方向可能相反，按 `(taxid, umls)` 聚合成一条边，
带 `n_studies` / `n_increased` / `n_decreased`，用方向一致性当 `weight`。

### 3.5 落地步骤

1. 先出 `nodes.csv` + `edges.csv` 两张表，**把对齐问题暴露在表里**，确认无误再导图库
2. 再用 `neo4j-admin import` 导入，建唯一性约束与索引
3. 建图顺序：Ingredient（枢纽，最难对齐，先做干净）→ Target → Herb → Disease/Microbe → 最后补 Target-Disease

**最关键的交付物是 `alignment_report.md`，不是图本身**——
逐类记录对上多少、丢了多少、为什么丢。3.2.3 那张表就该是它的内容。
对不上的实体绝不能悄悄丢掉，要计数并给出原因。
