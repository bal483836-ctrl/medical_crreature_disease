# 中药-成分-靶点-微生物-疾病 数据集总览

> 写于 2026-09-23。文中所有数字都是**从实际下载到的文件里数出来的**，不是抄论文的。
> 凡是和论文对不上的地方，我都标了出来——这类出入比想象中多。
> 数据在本仓库 `datasets` 分支，超过 90MB 的在 Releases。

---

## 一、整体情况

最初圈定了 9 个库，**实际拿到 5 个，另外 4 个拿不到**。拿不到的原因全是服务方的问题，
不是方法问题，下面会逐个说清楚。

拿到的 5 个，合计约 **1.14 GB**：

| 库 | 位置 | 体量 | 怎么拿到的 |
|---|---|---|---|
| SymMap | `symmap/` | 26 个 XLSX，21 MB | 官网直链（只有 http，https 拒连） |
| dbPTH | `dbpth/` + Releases | **932 MB** | 官网 `Download.php` 的分片 |
| Disbiome | `disbiome/` | 10,866 条，11.8 MB | BioThings 镜像（官网 API 是坏的） |
| TCMSP | `tcmsp/` | 4 张表，7.3 MB | `browse.php` 页面内联的整表 |
| TCMID | `tcmid/` | 4 个 zip，9.3 MB | Wayback 存档（**是 v1.0，不是 2.0**） |

没拿到的 4 个：

- **HERB** —— 下载接口是好的，但它指向的文件在服务端已经被删了。
  证据是同一个接口对不同路径写法给出两种错误：格式不对的回 `unavailable path`，
  格式对的回 `file_path dose not exists`。Wayback 里这个域唯一存档到的"数据文件"是 `robots.txt`。
- **HIT 2.0** —— 课题组自己的 Resources 页上给的唯一地址是 `hit2.badd-cao.net`，
  而那只是个指向 `badd-cao.net:2345` 的 frameset，那个端口 Connection refused。服务停了。
- **MDIPID** —— `mdipid.idrblab.net` 持续返回 HTTP 500，Wayback 零存档。
- **MicrobeTCM** —— Vue 单页应用，页面里没有任何数据链接；它的 `/api/index/*` 看着像接口，
  实际是**文件上传**用的，不是取数的。

丢掉 MicrobeTCM 和 MDIPID 的代价很具体：**"中药→微生物"这条边整个没了**。
这直接决定了后面知识图谱能做成什么样，第三节会讲。

### 关于数据真实性

过程中遇到过三次"看起来成功、其实是假数据"，都是靠打开文件才发现的：
Disbiome 最早那几个 JSON 其实是网站首页的 HTML；HERB 那 7 个文件是 24 字节的
`Sorry, unavailable path.`；HIT 2.0 从存档里拿到的是另一个工具（SEPPA3）的脚本包。
这些已经全部清掉了，现在 `datasets` 分支里的东西都逐个开过。

---

## 二、各个数据集

### 2.1 SymMap

**一句话**：中药领域少有的把"中医症状"和"现代医学症状"做了人工映射的库，
所以它能同时从分子和症状两头连接中西医。

- **网址**：http://www.symmap.org/ （下载页 `/download/`，**注意只有 http**）
- **所有者**：中国中医科学院、清华大学等（NAR 2019, 47(D1):D1110）
- **形式**：静态文件直接下载，无需注册
- **格式**：Excel `.xlsx`，每个实体一个文件；另配一份 `key file` 是站内检索词表，做分析用不上
- **数据量**（实测，v2.0）：

  | 表 | 内容 | v1.0 | v2.0 |
  |---|---|---:|---:|
  | SMHB | 草药 | 499 | **703** |
  | SMIT | 成分 | 19,595 | **27,690** |
  | SMTT | 靶点 | 4,302 | **20,965** |
  | SMDE | 疾病 | 5,235 | **14,434** |
  | SMTS | 中医症状 | 1,717 | **2,364** |
  | SMMS | 现代医学症状 | 961 | **1,148** |
  | SMSY | 证候 | — | **233** |

**真实样例**（`samples/symmap_v1_0_SMHB.real.tsv`）：

```
Herb_id  Chinese_name  Pinyin_name  Latin_name             Properties            Meridians     Class_Chinese
1        矮地茶         Ai Di Cha    Ardisiae japonicae     Mild,Pungent,Bitter   Lung,Liver    止咳平喘药
2        艾叶           Ai Ye        Folium Artemisiae Argyi Warm,Pungent,Bitter  Spleen,Liver,Kidney  温经止血药
```

疾病表（`samples/symmap_v1_0_SMDE.real.tsv`）自带一整排交叉引用：

```
Disease_id  Disease_Name  Disease_definition  MeSH_id  OMIM_id  Orphanet_id  ICD10CM_id  UMLS_id  MedDRA_id
```

**评价**：

优点是**交叉引用做得最全**，这点被低估了。靶点表自带 `HIT_id` 和 `TCMSP_id`，
疾病表自带 `MedDRA_id` + `UMLS_id` + `MeSH_id` + `ICD10CM_id`——
等于白送了一张跨库映射表，做融合时能省掉大量名称对齐的脏活。

但有两个必须知道的问题：

1. **下载页只有实体表，没有任何关系表**。我核过下载页全部 26 个链接，
   就是 6–7 张实体表加各自的 key file，再没别的。也就是说
   **光靠批量下载建不出 SymMap 的网络**，拿到的只是节点属性。
   关系数据只在网页检索结果里，得按 703 味草药逐个抓。
2. **论文里引用的数字全是 v1.0 的**。很多文章写"SymMap 收录 499 味草药、4,302 个靶点"，
   那是 v1.0；v2.0 靶点已经到 20,965，差了近 5 倍。**用 v2.0**，引用时注意标版本。

另外 v2.0 多了 `Version` / `Suppress` / `Link_*_id` 三个管理字段，
**载入时要先按 `Suppress` 过滤掉废弃记录**，否则统计会偏高。
还有个小坑：v1.0 的 `Symptom_pinyin_name` 在 v2.0 改名成了 `Symptom_pinYin`，跨版本合并会踩。

---

### 2.2 dbPTH 1.0

**一句话**：中药成分-靶点互作收录量最大的库，实验验证的互作数比同类库高一个量级。

- **网址**：http://dbpth.biocuckoo.cn/ （下载页是 **`/Download.php`**，写成 `/Download/` 会 403）
- **所有者**：华中科技大学 BioCuckoo 课题组 与 北京中医药大学（GPB 2026）
- **形式**：整库打包 + 按分类切好的分片
- **格式**：zip，解压后是制表符分隔的 `.txt`
- **数据量**：官方称 165,967 条互作 / 27,981 个靶点 / 4,856 个成分。
  实测我们取了 9 个 `ProtGrp` 分片 + 1 个 SQL dump，共 **932 MB**。

**这里有个很实用的发现**：下载页其实列了 **1,325 个 zip 分片**，
按蛋白分组（`ProtGrp/`）和按成分分类（`IngCls/`）两种切法。
**按 ProtGrp 取 9 个分片就覆盖全库，合计 586 MB，根本不用下 21.4 GB 的整包。**
要建数据库的话直接用 `PTH.sql.zip`（346 MB）更省事。

**真实样例**（`samples/dbpth_Secreted_Protein.real.tsv`）：

```
dbPTH ID     Protein Accession  Protein Name                   Gene   Ingredient            PubChem  Chemical Subtype  Protein Subgroups                  Specie
PTHT0000272  A0A0G2JGF1         Interleukin 6 receptor, alpha  Il6ra  Cysteine              5862     Small peptides    Secreted_Protein@Membrane_Receptor Mus musculus (Mouse)
PTHT0000272  A0A0G2JGF1         Interleukin 6 receptor, alpha  Il6ra  alpha-D-Mannopyranose 185698   Saccharides       Secreted_Protein@Membrane_Receptor Mus musculus (Mouse)
```

**评价**：

覆盖率确实是这批里最高的，光 `Secreted_Protein` 一个分片就有 **587,330 行、22,809 个蛋白、3,395 个成分**。

但实际字段比论文描述的**精简得多**。我原以为会有活性值、实验方法、PMID、同源组 ID，
**这些一个都没有**，只有 9 列。这直接改变了它的定位：
**dbPTH 告诉你"某成分和某蛋白有互作"这个事实，但不告诉你强度和证据细节。**
要 IC50/Kd 这类活性值，得靠 HIT 2.0（而它已经停服了）。

两个使用上的坑：

1. **物种混杂得厉害**。文献说覆盖 8 个物种，但单个分片里我就看到兔、山羊、犬、
   大熊猫、金丝猴、青鳉……**做人类研究必须先按 `Specie` 过滤**，否则会混进大量非人记录。
   同源关系不是靠某个字段给的，而是同一个 `dbPTH ID` 下挂多个物种的 `Protein Accession`。
2. `Protein Subgroups` 是 `@` 分隔的多值字段，不 split 的话按分组统计会漏。

---

### 2.3 Disbiome

**一句话**：专做"某种菌在某种病里变多还是变少"的库，标准化程度是这批里最好的。

- **网址**：https://disbiome.ugent.be/
- **所有者**：比利时根特大学（BMC Microbiology 2018, 18:50）
- **形式**：REST API
- **格式**：JSON（BioLink 风格的嵌套结构）
- **数据量**（实测）：**10,866 条实验记录，1,537 个微生物（按 taxid），323 种疾病（按 MedDRA）**

**取数方式要特别说一下**：官网那套 `/api/disbiome/experiments` 接口**是坏的**——
它对任何路径都回 HTTP 200 + 网站首页的 HTML。只看状态码会以为下载成功了，
实际存下来一堆 HTML。真正能用的是 BioThings 镜像：

```
https://pending.biothings.io/disbiome/query?q=__all__&fetch_all=true
```

单次查询上限 1000 条，**必须用 `fetch_all` 拿 scroll_id 翻页**，不然只拿到第一页还以为是全量。

**真实样例**（`samples/disbiome_experiments.real.tsv`，已扁平化）：

```
organism        taxid   rank    disease                     meddra_id  qualifier  sample_site  method                host   pmid
oribacterium    265975  genus   invasive colorectal cancer  10052358   decreased  feces        16S rRNA sequencing   human  31609493
paraprevotella xylaniphila 454155 species  attention deficit hyperactivity disorder 10003736 increased feces MiSeq sequencing human 32132899
```

**评价**：

标准化是真的好：疾病挂 MedDRA、微生物挂 NCBI Taxonomy，还带 PMID 和 DOI。
尤其 `subject.lineage` 直接给了完整的 NCBI 分类谱系数组，
**要把种级记录上卷到属级或门级不用另外查分类库**，这个字段很省事。

三个使用上要注意的：

1. **粪便样本只占一半**（5,500/10,866），另外近一半来自口腔、皮肤、泌尿生殖道、
   支气管肺泡灌洗液等。**做肠道菌群研究必须先按 `sources` 过滤**，
   否则会把口腔菌群的结论混进来。
2. **检测方法名没有归一化**。`16S rRNA sequencing`、`16S rDNA pyrosequencing`、
   `16S rRNA pyrosequencing`、`MiSeq sequencing` 实际都是 16S 扩增子测序，
   按方法分层分析前得自己合并同类项。
3. 方向只有 `increased` / `decreased` 两极（5,713 / 5,153），**没有效应量**。
   同一个菌-病对经常有多条来自不同研究的记录，方向还可能打架——这点在建图时要处理。

---

### 2.4 TCMSP 2.3

**一句话**：中药网络药理学的事实标准入门库，价值不在数据量而在它是唯一系统提供 ADME 参数的。

- **网址**：https://www.tcmsp-e.com/
- **所有者**：西北农林科技大学（J Cheminform 2014, 6:13）
- **形式**：网页；**但不需要爬虫**，见下
- **格式**：页面内联 JSON（我们另存了一份 TSV）
- **数据量**（实测）：草药 **502**、成分 **13,729**、靶点 **3,339**、疾病 **867**

**取数方式**：这个库常被认为"只能逐草药爬"，我一开始也是这么判断的，**这个判断是错的**。
`browse.php` 把整张表直接内联在 Kendo Grid 的 `dataSource.data` 里，
四次请求就拿到四张主表：

```
browse.php?qc=herbs / ingredients / targets / diseases
```

**真实样例**（`samples/tcmsp_ingredients.real.tsv`）：

```
MOL_ID     molecule_name  mw       hdon hacc alogp halflife  ob             caco2    bbb       dl       tpsa    rbn
MOL000098  quercetin      302.250  5    7    1.510 14.400548 46.4333481195  0.04842  -0.76890  0.27525  131.35  1
MOL001454  berberine      336.390  0    4    3.450 6.565936  36.8612450372  1.24179  0.56718   0.77665  40.80   2
```

疾病表还自带 ICD 编码，这点我之前没注意到：

```
disease_ID  DIS_ID    disease_name               ICD9   ICD10
1           DIS00001  Abdominal aortic aneurysm  442    172
```

**评价**：

ADME 参数是它不可替代的地方——OB（口服生物利用度）、DL（类药性）、Caco-2、BBB、
半衰期等十来项，领域里人人写的那句"以 OB ≥ 30%、DL ≥ 0.18 筛选活性成分"就出自这里。
我抽查了几个知名成分，quercetin OB=46.43/DL=0.275、berberine OB=36.86/DL=0.778，
**和 TCMSP 公布值一致**，数据是对的。套用那个惯例阈值，**13,729 个成分里剩下 2,583 个（18.8%）**，
这就是实际的起点规模。

**一个很容易中招的坑**：JSON 里所有数值字段都是**字符串**（`"ob":"46.4333481195"`），
直接拿去比大小会静默出错——字符串比较下 `"9" > "30"`。必须先转数值：

```python
df[["ob","dl","mw","caco2","bbb"]] = df[["ob","dl","mw","caco2","bbb"]].apply(pd.to_numeric, errors="coerce")
```

好消息是 13,729 个成分的 `ob`/`dl` 全都能成功转换，没有缺失；但 `halflife` 有空值，要容错。

局限：这四张是**实体表，不含关系**。成分↔靶点、靶点↔疾病的配对数据在
`tcmspsearch.php?qr=<名称>&qsr=herb_en_name&token=<token>` 的逐草药结果页里，
本次没抓。另外它的靶点有一部分是模型预测的，做机制论证时该和 dbPTH 的实验证据交叉验证。

---

### 2.5 TCMID（v1.0）

**一句话**：唯一系统收录**复方**的库，而且它是这批里少数直接提供关系数据的。

- **网址**：原址 `tcmid.org` **已经不是这个库了**——域名过期后被人注册成了一个叫
  "My Sheep Gate" 的慈善机构网站。历史镜像 `megabionet.org/tcmid/` 超时。
  我们的数据来自 **Internet Archive 存档**，来源和快照时间记在 `tcmid/provenance.json`。
- **所有者**：上海交通大学等（TCMID 2013 / 2.0 见 NAR 2018, 46(D1):D1117）
- **形式**：存档恢复的静态文件
- **格式**：zip，解压后是制表符分隔的 `.csv`
- **数据量**（实测）：

  | 文件 | 行数 | 内容 |
  |---|---:|---|
  | `prescription.csv` | **46,914** | 复方：中文组成、主治、用法、出处 |
  | `compounds.csv` | **32,281** | 成分：STITCH ID、PubChem ID、分子式、SMILES |
  | `compound_protein.csv` | **211,152** | 成分→蛋白（UniProt） |
  | `herb.csv` | **8,203** | 草药：药性、归经、用部、功效、主治、所含成分 |

> ⚠️ **这是 v1.0，不是 2.0。** 存档快照是 2015 年，文件日期 2012。
> 论文里 2.0 的统计数字（18,203 成分等）**不适用于这批文件**。

**真实样例**（`samples/tcmid_prescription.real.tsv`）：

```
Pinyin Name           Chinese Name  Composition                                        Indication              Method
JIA WEI CAN XIA TANG  加味参夏汤      人参1两半，半夏1两半，肉桂1两，甘草（炙）5钱，乳香3钱。  七情相干，眩晕欲倒...     每服5钱，加生姜5片，水煎服。
```

草药表（`samples/tcmid_herb.real.tsv`）：

```
Pinyin Name    Chinese Name  Latin Name                 Properties                    Meridians      Use Part   Ingredients
TIAN MEN DONG  天门冬         Asparagus cochinchinensis  Sweet, bitter,Extremely cold  Lung,kidney    tuberoid   2300,E|2303,E|...
```

**评价**：

对建图来说，**TCMID 的价值被严重低估了**——它是这 5 个库里少数直接给"边"的。
`herb.csv` 的 `Ingredients` 列就是草药→成分的边，`compound_protein.csv` 就是成分→蛋白的边。
我验过它的内部 ID 连接，**干净得出乎意料**：

- herb→ingredient 共 **59,805 条引用，100% 能解析**到 `compounds.csv`
- `compound_protein.csv` 涉及的 **4,939 个成分 ID，100% 能对上**

但有三个必须打折扣的地方：

1. **成分-蛋白的证据等级低**。来源列显示 **205,939 条来自 STITCH、5,213 条来自 Textmining**，
   而 STITCH 本身就混合了预测、共表达和文本挖掘。
   **这 21 万条边不能和 dbPTH 的实验验证边同等对待**，建图时必须分权重。
2. **草药表的注释非常稀疏**。8,203 行里只有 **1,851 行**有中文名，
   有药性的更少，只有 **848 行**，其余大量是 `NA`。
   所以**别指望用它做药性/归经分析**——那部分该用 SymMap。
   但要注意：**`Ingredients` 列是 8,203 行全有的**，也就是说它作为"边"的来源是完整的，
   稀疏的只是节点属性。这正好符合它在图里的定位：提供边，属性交给 SymMap。
3. **只有 36.9% 的成分有 PubChem ID**（11,896/32,281）。这直接影响跨库对齐——
   剩下六成得靠 SMILES 自己算 InChIKey 才能接上别的库。

---

## 三、怎么融合成知识图谱

### 3.1 先说结论：现在这 5 个库建出来的图是**断开的**

这是最要紧的一点，先讲清楚再谈方案。

能建起来的是两个**互不相连**的子图：

```
子图 A（中药侧）：  Herb ──含有──> Ingredient ──作用于──> Target/Protein
子图 B（微生物侧）： Microbe ──关联──> Disease
```

缺的是把两边接起来的边：

- **Target → Disease**：SymMap 只给实体表不给关系；TCMSP 的关系在逐草药页里，本次没抓。
- **Herb/Ingredient → Microbe**：这条边本来在 MicrobeTCM 和 MDIPID 里，而这两个库没拿到。

后果很具体：现在这个图能回答**"某成分作用于哪些靶点"**、
**"哪些菌和某种病相关"**，但**回答不了"某味中药调节哪种菌"**——
而后者恰恰是最初想做的那条完整链条。

**补边的两个办法**（按性价比排序）：

1. **抓 TCMSP 的关系页**，补上 Target→Disease。机制已经摸清了：
   `tcmspsearch.php?qr=<草药英文名>&qsr=herb_en_name&token=<token>`，
   token 在 `browse.php` 页面里就能取到。502 味草药逐个请求，限速跑一轮就行，规模不大。
   补上这条边之后，两个子图就能通过 **Disease 节点**连起来——
   虽然是间接的（"某中药作用于某病，而该病又与某菌相关"），但链条通了。
2. **直接向作者索取 MicrobeTCM / MDIPID 的数据**。这两个都是人工策展的中等规模库
   （MicrobeTCM 是 419 篇文献双人提取），写信要通常比逆向前端快，也更规范。
   拿到后才能有真正的 Herb→Microbe 直接边。

### 3.2 节点设计与主键选择

| 节点 | 主键 | 理由 |
|---|---|---|
| `Ingredient` | **PubChem CID**，缺失时用 SMILES 算 InChIKey | 中药成分同名异物、异名同物太普遍，**绝对不能用名称当主键**。TCMID 只有 36.9% 有 CID，所以 InChIKey 兜底是必需的，不是可选项 |
| `Target` | **UniProt Accession** | dbPTH 和 TCMID 都直接给 UniProt；基因符号只作为显示属性，不当主键（符号会改名） |
| `Disease` | **UMLS CUI** | SymMap 的 SMDE 同时带 UMLS + MedDRA + MeSH + ICD10CM，天然适合当枢纽 |
| `Microbe` | **NCBI taxid** | Disbiome 直接给 taxid 和完整 lineage |
| `Herb` | SymMap `Herb_id` | 用 SymMap 当基准，因为它自带 `TCMSP_id` 和 `TCMID_id`，能直接搭桥 |

### 3.3 跨库对齐的具体走法

**这是整个融合里最省事的一点：SymMap 已经把大部分映射关系白送了。**

- **草药**：SymMap 的 `SMHB` 表里有 `TCMSP_id` 和 `TCMID_id` 两列，
  直接拿来当映射表，不用去做拉丁名/拼音的模糊匹配。剩下对不上的再用 `Latin_name` 兜底。
- **靶点**：SymMap 的 `SMTT` 表带 `UniProt_id`，dbPTH 的 `Protein Accession`、
  TCMID 的第二列都是 UniProt，直接 join。
- **疾病**：SymMap 的 `SMDE` 自带 `MedDRA_id`，而 **Disbiome 的疾病正好也是 MedDRA 编码**，
  所以**这两家可以直接对上，不需要绕 UMLS 做映射**（这点我核过真实文件，是确定的）。
  TCMSP 的疾病只有 ICD9/ICD10，要经 UMLS 转一道。
- **成分**：先用 PubChem CID 直连；TCMID 那六成没有 CID 的，用 `SMILE String` 算 InChIKey，
  再和其他库用 SMILES 算出的 InChIKey 对。注意统一用 InChIKey 的前 14 位（骨架层）
  还是全长，前者会把立体异构体合并，看需求定。

### 3.4 边从哪来，以及怎么定权重

| 边 | 来源表 | 实测规模 | 证据等级 |
|---|---|---:|---|
| `Herb -含有-> Ingredient` | TCMID `herb.csv` 的 `Ingredients` 列 | 59,805 | 中（文献策展） |
| `Ingredient -作用于-> Target` | **dbPTH** 分片 | 单分片即 587,330 行 | **高（实验验证）** |
| `Ingredient -作用于-> Target` | TCMID `compound_protein.csv` | 211,152 | **低**（97.5% 来自 STITCH，2.5% 文本挖掘） |
| `Microbe -关联-> Disease` | Disbiome | 10,866 | 中（单篇实验观察） |
| `Ingredient` 的 ADME 属性 | TCMSP `ingredients` | 13,729 | — |
| 各类节点属性 | SymMap 各实体表 | — | — |

**必须给边打证据等级**，不能一视同仁。最典型的就是上面那两行成分-靶点边：
dbPTH 是实验验证的，TCMID 那 21 万条绝大部分来自 STITCH 的预测/共现。
把它们混成一种边，下游任何富集分析都会被稀释掉。建议在边上放 `evidence` 和 `source` 两个属性，
查询时按需过滤。

**Disbiome 的方向冲突要处理**：同一个 `(taxid, meddra)` 对经常有多篇研究，
方向可能一个说增加一个说减少。建议聚合成一条边，属性带上
`n_studies`、`n_increased`、`n_decreased`，再用方向一致性（多数方向占比）当置信度。
只取单条记录会让图看起来比实际证据更确定。

### 3.5 落地建议

不建议一上来就上图数据库。先出**两张 CSV**——节点表和边表——把对齐问题都暴露在表里，
确认无误再导入。这样出问题好查，也方便反复重跑。

节点表大致长这样：

```
node_id            label       name           xrefs                                   props
INGR:PUBCHEM:5280343  Ingredient  quercetin    pubchem=5280343;inchikey=REFJWTP...    ob=46.43;dl=0.275
TGT:UNIPROT:P35354    Target      PTGS2        uniprot=P35354;symbol=PTGS2            species=human
DIS:UMLS:C0009402     Disease     colorectal cancer  meddra=10010029;icd10=C18        -
MIC:NCBI:851          Microbe     Fusobacterium nucleatum  taxid=851                  rank=species
```

边表：

```
src                   rel              dst                  evidence      source   weight
INGR:PUBCHEM:5280343  TARGETS          TGT:UNIPROT:P35354   experimental  dbPTH    1.0
INGR:PUBCHEM:5280343  TARGETS          TGT:UNIPROT:P35354   predicted     TCMID    0.3
MIC:NCBI:851          ASSOCIATED_WITH  DIS:UMLS:C0009402    observational Disbiome 0.85
```

导入 Neo4j 之后，一个典型查询会是这样（注意它现在**只能**在子图 A 内部走）：

```cypher
MATCH (h:Herb)-[:CONTAINS]->(i:Ingredient)-[r:TARGETS]->(t:Target)
WHERE r.evidence = 'experimental' AND toFloat(i.ob) >= 30 AND toFloat(i.dl) >= 0.18
RETURN h.name, collect(DISTINCT t.symbol) AS targets
```

等 3.1 节说的 Target→Disease 边补上，才能接着往 Disease 走到 Microbe，
那时候完整链条的查询才写得出来。

### 3.6 建图顺序

1. 先建 `Ingredient` 节点——它是整张图的枢纽，也是对齐最麻烦的一类，先把它做干净
2. 再挂 `Target`（UniProt 直连，最容易）
3. 然后 `Herb`（用 SymMap 的 `TCMSP_id`/`TCMID_id` 搭桥）
4. `Disease` 和 `Microbe` 单独建，先形成子图 B
5. 最后补 Target→Disease，把两个子图接上

每一步之后都记一下"对上了多少、丢了多少"。对齐率是这类融合里最该盯的指标，
尤其是 TCMID 那六成没有 PubChem ID 的成分——它们的去向直接决定复方那部分数据能不能用上。

---

## 附：数据位置

- **数据**：`datasets` 分支，每库一个目录；`STATUS.md` 是持有清单
- **大文件**：Releases（dbPTH 的 `PTH.sql.zip` 346MB、`Enzyme.zip` 298MB、`Other.zip` 126MB）
- **真实样例**：主分支 `samples/*.real.tsv`（`*.example.*` 是未取得的库的占位，别当数据用）
- **字段详解**：主分支 `docs/`
